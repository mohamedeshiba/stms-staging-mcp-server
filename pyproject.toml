[project]
name = "stms-mcp"
version = "0.1.0"
description = "MCP Server for sc-stms-api Staging Environment"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.26.0",
]
